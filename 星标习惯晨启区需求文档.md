# 星标习惯晨启区 需求文档

## 1. 需求背景

用户在早晨习惯养成过程中，需要优先处理最重要的晨间动作。高频关键习惯应该在打卡页面顶部突出显示，帮助用户建立"先做最重要的事"的晨间仪式感。

## 2. 功能目标

- 支持将高频习惯设为星标
- 在今日打卡页顶部生成"今日先做"专属区域
- 星标项可单独调整顺序
- 未完成前始终置顶，完成后自动下沉
- 帮助用户先处理最关键的晨间动作

## 3. 功能详情

### 3.1 星标标记

- **入口**：习惯清单页每个习惯项右侧、习惯编辑表单
- **交互**：点击星标图标切换星标状态
- **视觉**：
  - 已星标：金色实心星（⭐）
  - 未星标：灰色空心星
- **数据**：习惯表增加 `starred` 布尔字段

### 3.2 今日先做区域

- **位置**：今日打卡页顶部，页头下方
- **展示条件**：至少有一个星标习惯时显示
- **标题**：⭐ 今日先做
- **卡片样式**：
  - 左侧 3px 金色边框突出显示
  - 习惯名称旁带 ⭐ 徽章
  - 与普通习惯卡片视觉区分

### 3.3 智能排序

**排序规则（星标区域内）：**
1. 未完成的习惯 → 排在前面（置顶）
2. 已完成的习惯 → 排在后面（下沉）
3. 同一状态内 → 按用户设置的排序号排列

**效果：**
- 用户完成一个星标习惯后，它自动沉到星标区域底部
- 未完成的关键习惯始终保持在最显眼的位置

### 3.4 拖拽排序

- **入口**："今日先做"区域右上角的"排序"按钮
- **模式切换**：
  - 点击"排序" → 进入排序模式，显示拖拽手柄
  - 点击"完成" → 退出排序模式，保存顺序
- **交互**：
  - 排序模式下可长按/拖拽调整顺序
  - 实时预览排序效果
- **数据**：习惯表增加 `sort_order` 整数字段

### 3.5 其他习惯分区

- 星标习惯下方显示"其他习惯"分隔线
- 分隔线下方展示所有非星标习惯
- 视觉层次清晰，突出重点

## 4. 后端接口

### 4.1 切换星标

```
POST /api/habits/{id}/star
```

**响应：** 返回更新后的习惯对象

### 4.2 更新星标排序

```
PUT /api/habits/starred/order
Content-Type: application/json

{
  "habitIds": [1, 4, 2]
}
```

**说明：** 按传入的 ID 顺序批量更新星标习惯的排序号

### 4.3 习惯列表排序规则

`GET /api/habits` 返回的列表按以下优先级排序：
1. 星标降序（starred DESC）
2. 排序号升序（sort_order ASC）
3. 创建时间降序（create_time DESC）

## 5. 数据库变更

### habit 表新增字段

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| starred | TINYINT(1) | 0 | 是否星标 |
| sort_order | INT | 0 | 排序号（星标习惯有效） |

### 新增索引

- `idx_starred` - 星标字段索引，提升星标筛选性能

### 数据迁移

- 应用启动时自动检查并添加缺失字段
- 首次迁移时将"早起"、"喝8杯水"设为默认星标习惯
- 迁移逻辑幂等，重复执行不影响数据

## 6. 前端实现要点

### 6.1 状态管理（Pinia）

- `starredHabits` getter：筛选并排序星标习惯
- `nonStarredHabits` getter：筛选非星标习惯
- `toggleStarred(id)` action：切换星标状态
- `updateStarredOrder(ids)` action：更新排序
- `moveStarredHabit(from, to)` action：移动位置

### 6.2 Checkin.vue 页面结构

```
页面
├── 页头（标题、完成率环）
├── 今日先做区域（条件渲染）
│   ├── 区域标题 + 排序按钮
│   └── 星标习惯列表
├── 其他习惯分隔线（条件渲染）
├── 普通习惯列表
└── 添加按钮
```

### 6.3 拖拽实现

- 使用原生 HTML5 Drag and Drop API
- 排序模式下显示拖拽手柄图标
- 拖拽过程中实时重排
- 退出排序模式时统一保存

## 7. 验收标准

- [ ] 习惯清单页可正常切换星标状态
- [ ] 星标习惯在今日打卡页顶部"今日先做"区域展示
- [ ] 点击星标习惯可正常打卡
- [ ] 未完成的星标习惯排在星标区域前面
- [ ] 已完成的星标习惯自动沉到星标区域后面
- [ ] 可通过拖拽调整星标习惯的顺序
- [ ] 非星标习惯在"其他习惯"分区正常展示
- [ ] 添加习惯时可选择是否设为星标
- [ ] 数据库字段自动迁移，旧数据不报错
- [ ] 重启应用后排序状态保持

## 8. 文件清单

### 后端
- `backend/src/main/resources/schema.sql` - 建表脚本更新
- `backend/src/main/java/com/habit/entity/Habit.java` - 实体新增字段
- `backend/src/main/java/com/habit/dto/HabitDTO.java` - DTO 新增字段
- `backend/src/main/java/com/habit/service/HabitService.java` - 业务逻辑
- `backend/src/main/java/com/habit/controller/HabitController.java` - 接口
- `backend/src/main/java/com/habit/config/DatabaseMigrationConfig.java` - 自动迁移
- `backend/pom.xml` - 新增 jdbc 依赖

### 前端
- `frontend/src/views/Checkin.vue` - 今日打卡页改造
- `frontend/src/store/habit.js` - 状态管理（已有，需确认 getter 逻辑）
- `frontend/src/utils/request.js` - API 接口扩展
- `frontend/src/views/Habits.vue` - 习惯清单页（已有星标功能）
